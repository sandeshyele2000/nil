<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name=".ui.NILInspectorActivity"
            android:exported="false" />

        <provider
            android:name="com.sandesh.nil.provider.NILFileProvider"
            android:authorities="${applicationId}.nil.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/nil_file_paths" />
        </provider>
    </application>
</manifest>
